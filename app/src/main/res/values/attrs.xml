<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>
    
    <declare-styleable name="CustomView">
        <attr name="showText" format="boolean"/>
        <attr name="lab" format="enum">
            <enum name="left" value="0"/>
            <enum name="right" value="1"/>
        </attr>
    </declare-styleable>
    
    <declare-styleable name="TouchPullView">
        <attr name="maxPullHeoght" format="integer"></attr>
        <attr name="minWidthPull" format="integer"/>
        <attr name="circleRadius" format="integer"/>
        <attr name="max_degree"  format="integer"/>
        <attr name="control_height" format="integer"/>
        <attr name="color" format="color"/>
    </declare-styleable>

</resources>
